the code that is being presented does not show all the parts of the game,
as godot uses things called nodes in order to help create things such as 
sprites for is GUI. if this code does not seem to be repersentative of the
project that is because parts of the project were only done by nodes.
